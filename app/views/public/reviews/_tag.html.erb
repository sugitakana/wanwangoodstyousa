<h5 class="subtitle mt-3">タグ検索</h5>
<%= form_with(url: tag_path(tag.id), local: true, method: :get ) do |f| %>
 <%= f.collection_select :tag_id, tags, :id, :name,{prompt: "--選択してください--"} %>
 <%= f.submit "検索" %>
<% end %>