<%= form_with(model:[review, comment]) do |f| %>
  
  <%= f.text_area :comment, placeholder: "コメントをここに", class: "form-control" %>
  <div class="text-center">
   <%= f.submit "送信する", class: "mt-3 btn btn-primary" %>
  </div>
<% end %>
